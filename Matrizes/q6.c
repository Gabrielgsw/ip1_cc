#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main(void){

    int rows = 5, int columns = 4,int tamanho = 30;
    int info[rows][columns];
    char nomes[rows][tamanho]


    for(int i = 0;i<rows;i++){
        printf("Digite o nome do aluno %i: ",(i+1));
        scanf("%s",nomes[i]);
    }


    for(int i = 0;i<rows;i++){
        for(int j = 0;j<columns;j++){
            switch (j){
                case 0:
                    printf("Digite o numero de matricula: ");
                    scanf("%i",&)
            }
                
        }
    }
}